import './formation.css'

import React from 'react';

const Formation = () => {
    return (
        <section id="formation">
            Formation
        </section>
    );
};

export default Formation;